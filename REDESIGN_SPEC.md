# REDESIGN_SPEC — 3계층 돌봄 운영 시스템 (P0 프리즈)

이 문서는 3계층 재설계 작업의 동결(frozen) 사양이다. `OVERNIGHT_SPEC.md`는 참조 자료로만
유지하며, 이번 재설계의 페이즈 완료 조건과 불변식은 이 문서가 규범이다.

## 철학

행정 계층이 세 개라서 화면이 세 개다.

| 계층 | 화면 | 핵심 과업 |
| --- | --- | --- |
| 시·구 (배치) | `/city` | 구 단위 구조 맥락·운영 부하 브리핑, 증원 검토 |
| 동 행정복지센터 (확인·승인) | `/center` | 보고 확인, 오늘 배치 확인, 방문 승인·반려, 이관 |
| 조사원 (현장) | `/m` | 오늘 연락·방문, 통화 결과 입력(3경로), 보고 제출 |

제품 법칙 하나로 통일한다: **AI와 결정론 규칙은 '제안'만 만들고, 확정은 전부 사람의
명시적 액션이다.**

백엔드 엔진(`backend/src/contact-ops.mjs`, `backend/src/contact-triage-scoring.mjs`)은
동결(INV1)이며, 이번 작업은 뷰·어댑터 재배치다.

## 격리·브랜치 (환경 확정 사항)

- 작업 브랜치: `claude/three-tier-care-redesign-8lpmkw`
  (원 스펙의 `agent/three-tier` 명칭은 실행 환경이 지정한 위 브랜치로 대체한다.
  다른 브랜치로의 푸시는 금지.)
- 페이즈마다 커밋. 드래프트 PR은 작업 브랜치에서 연다.
- 프리뷰 URL: 배포 계약상 PR에는 Vercel/GCP 자격 증명이 주어지지 않는다
  (`docs/DEPLOYMENT.md`). 따라서 페이즈별 "프리뷰 URL"은 로컬 프로덕션 빌드
  (`npm run build` + preview)와 페이즈별 스크린샷(390×844·1440×900)으로 대체하고,
  실제 Vercel 프리뷰는 사람이 수동 배포로만 만들 수 있음을 HANDOFF에 미증명으로 기록한다.
- 검증 Node: 저장소 계약은 Node 24 (`engines >=24 <25`). 이 컨테이너의 기본 Node 22에서는
  백엔드 커버리지 게이트 계측이 달라 실패하므로, 모든 검증은 `/opt/node24/bin`을 PATH
  앞에 두고 실행한다.

## 불변식

기존 `OVERNIGHT_SPEC.md`의 INV1~INV13은 전부 유지된다. 특히:

- INV1 엔진 동결: `git diff --exit-code 1fab6889 -- backend/src/contact-ops.mjs backend/src/contact-triage-scoring.mjs`
- INV2 두 축 분리(합산 점수 금지), INV6 담당자만 승인/반려, INV7 회귀 래칫(테스트
  삭제·약화·건수 감소 금지), INV4 합성 정체성 경계.

여기에 다음을 추가한다.

### INV14 — 제안과 확정의 분리
모든 자동 산출(할당 제안·기관 권고·체크리스트 후보)은 '제안(proposed/candidate)'
상태로만 생성된다. 확정은 명시적 사람 액션(할당 확인, 체크리스트 확정 후 제출,
방문 승인)만이 만든다. 백엔드 테스트로 단언한다: 제안 생성 API는 어떤 경로로도
`confirmed`/`approved`를 만들지 못한다.

### INV15 — 가상 전화번호
UI·API에 노출되는 전화번호는 `010-0000-XXXX` 형태의 가상 번호 + `[가상]` 라벨뿐이다.
실번호 패턴은 grep 게이트로 금지한다.

**INV4와의 조정(확정 결정):** INV4는 "생성기·픽스처는 전화번호를 만들지 않는다"를
동결한다. 따라서 가상 번호는 픽스처에 쓰지 않고, 어댑터 계층이 케이스 ID로부터
결정적으로 유도해 응답 시점에만 부여한다(`010-0000-` + 케이스 ID 결정 함수, 항상
`[가상]` 라벨 동반). 픽스처·생성기·스키마는 무수정. 이 유도는 순수 함수이며 테스트로
결정성을 단언한다.

### INV16 — 전화/방문 레이어 분리
방문 레인에는 (a) 승인된 방문 또는 (b) 방문 선호(`preferred_contact_method=visit`)
due 업무만 들어간다. 전화 큐와 방문 레인은 혼합 렌더하지 않으며, 프런트 테스트와
백엔드 레인 분리 API 테스트로 단언한다.

### INV17 — 시·구 뷰 개인정보 최소화
`/city`에서는 개별 케이스 ID·케이스 상세에 접근할 수 없다. 지도 클릭 포함 모든 상호작용은
동 단위 롤업까지만 내려간다. 시·구 집계 API 응답에 케이스 ID 필드가 없음을 테스트로
단언한다(최소수집·목적제한).

### INV18 — 등급 어휘 통일
"위험군" 단어는 UI에서 금지(grep — `scripts/check-ui-copy.mjs` 금지어에 추가)한다.
등급 어휘는 동결 엔진의 급성도 등급(`정상`/`주시`/`방문권고`/`방문권고-우선`)만 사용한다.

### INV19 — 구 단위 AI 요약 계약
구 단위 AI 요약은 (a) 서버가 주입한 집계 수치의 인용을 필수로 하고, (b)
`[AI 생성 · 관측 집계 해석 · 개인 예측 아님]` 라벨을 항상 동반하며, (c) 혼합 스냅샷
경고를 유지한다. 수치 계산은 서버(결정론)가 하고, OpenAI Codex가 작성한 구·군별
해석 문장에는 서버 집계값만 주입한다. 런타임 API 키나 모의 생성기는 사용하지 않는다.
작성본 버전과 모든 구·군 문장, 숫자 화이트리스트를 골든 테스트로 고정한다.

### 골든 갱신 규칙
IA 변경으로 기존 E2E의 문자열·경로 수정은 허용하되, 단언 수·강도 축소는 금지(INV7
래칫 유지). 기존 골든은 삭제가 아니라 신규 동선으로 재작성한다.

## 페이즈와 완료 조건

### P0 — 프리즈
- [x] 회귀 베이스라인 그린: Node 24에서 `npm run agent:check` 통과(2026-08-13 확인).
- [x] 이 문서(REDESIGN_SPEC.md) 커밋.

### P1 — 데이터·API 델타 (엔진 무수정, 어댑터 계층만)
새 어댑터 모듈(`backend/src/three-tier-*.mjs`)과 라우트를 추가한다. done 조건:

1. 가상 전화번호: 케이스 ID → `010-0000-XXXX` 결정적 유도 + `[가상]` 라벨. 응답 시점
   부여(INV15 조정 참조). 결정성·패턴 테스트.
2. 자동 할당 제안 엔진(결정론): due일·급성도 등급·동 매칭·연결단원 용량
   (`max_daily_approved_visits`, 시간창) → 할당 '제안' 배치 생성. 확인·조정 API는
   명시적 액션만 확정(INV14 테스트).
3. 보고 이벤트 모델: 조사원 제출(기존 contact-results 결과) → 구조화 보고 카드
   (케이스·등급·사유 기여내역 요약·evidence·권고 기관) 파생. 동 센터 인박스 피드 API.
4. 기관 권고 매핑(결정론 룰 테이블, 매뉴얼 근거 인용): 신호 조합 → 보건소·의료 /
   정신건강복지센터 / 구 희망복지지원단 / 현장 확인. '권고+사유'까지만(INV14).
5. 구 단위 집계 endpoint: 구별 노인비율·1인가구비율·기초수급 밀도·복지시설 수·
   운영 부하(due/overdue/승인대기 per 연결단원) 롤업. 케이스 ID 미포함(INV17 테스트).
6. 구 단위 AI 요약 endpoint: 서버가 집계 수치 주입 → Codex 작성 구·군별 해석문(INV19).
   작성본 버전·전 구·군 존재·숫자 화이트리스트 골든 테스트. 런타임 API 키 없음.
7. 백엔드 커버리지 게이트 유지(INV7), `npm run agent:check` 그린.

### P2 — 동 행정복지센터 웹 (`/center`)
텍스트 중심, 지도는 완전 서브. done 조건:

1. 메인 = 인박스 피드: 보고 카드(등급·사유·권고 기관·[확인][이관][방문검토] 액션),
   할당 제안 카드(오늘 배치안 → 일괄 확인/개별 조정).
2. 전화 레인/방문 레인 탭 분리(INV16 프런트 테스트).
3. 방문 승인·반려(담당자·거리·메모)·이관 처리는 기존 API 재사용. 이관 워딩은
   "안부확인 트랙 → 사례관리·전문기관 트랙 전환".
4. 지도는 접힌 사이드 위젯(우리 동 케이스 위치 확인용)만.
5. 헤더 "동 행정복지센터용" 명시, 처리 완료율·대기 수 상단 요약.
6. `check:ui-copy`·typecheck·build·기존 테스트 그린.

### P3 — 조사원 모바일 개편 (`/m`)
- 오늘 목록: 전화 탭/방문 탭 분리(INV16). 방문 탭엔 동 단위 위치·지도·선호 시간창·
  동행 요건. (합성 데이터에는 주소가 없다 — INV4. "주소" 항목은 동 단위 위치 표기로
  대체하고 그 사실을 화면에 명시한다.)
- 대상 정보: 가상 전화(탭하면 발신 UI 모사, 실제 발신 없음 명시), 마지막 연락, 등급.
- **개정(2026-08-13, main #25 머지 반영):** 픽스처가 공공 주거용 건물 도로명 주소
  참조(`road_address`, `not_real_resident=true`)를 갖게 되어, "주소" 항목은 동 단위
  대체 표기 대신 이 공공 건물 주소를 표시하되 "실제 거주자와 연결되지 않음"을
  항상 병기한다.
- 입력 3경로가 같은 체크리스트로 수렴:
  ① 음성: 파일 업로드(3b 계약) → 전사 → 기존 P3 후보 API → 체크리스트 후보 채움
  ② 챗봇: 짧은 문답으로 6징후·식사·위생 등 후보 채움(후보만, 결정론 문답)
  ③ 수동: 체크리스트 직접 체크
  → 어느 경로든 조사원이 확정해야 제출(INV14) → 기존 결정론 점수·규칙 →
  보고 카드 생성(등급+사유+권고 기관 미리보기) → 동 센터 피드로.
- 제출 후 "동 행정복지센터에 보고됨" 확인 화면.
- 고령 조사원 UI 룰: ≥18px 본문, ≥48px 터치 타깃, 한 화면 한 과업.

### P4 — 시·구 웹 (`/city`)
- 인천 전체 지도(기존 공개 P1/P2 + 운영 오버레이 유지). 케이스 클릭은 동 롤업까지만
  (INV17 — 케이스 ID 비노출 프런트 테스트).
- 구 단위 AI 분석 요약 패널(구 선택 → 집계 인용 해석문 + INV19 라벨).
- 증원 검토 패널: 구조 맥락(노인·1인가구·기초수급·시설 — 취약도 0~50 기여내역 재사용)과
  운영 부하(업무/연결단원)를 **나란히, 비합산** 표기 → "증원 검토 후보" 정렬.
  "배치 최적화"라 부르지 않는다.

### P5 — 데모 봉합
- 신규 동선 E2E 골든 재작성: 시 지도 → 동 센터 할당 확인 → 모바일 음성/수동 입력 →
  점수 상승 → 보고 카드 → 방문 승인 → 시 지도 반영. 단언 수·강도는 기존 골든 대비
  축소 금지.
- 데모 시나리오 시딩, 전 화면 스크린샷(390×844·1440×900) `artifacts/`에 저장.
- `MORNING_HANDOFF.md` 갱신: 증명/미증명 분리(실기기·워딩·664 튜닝 경고·
  프리뷰 URL은 미증명 유지).
- 푸시 + 드래프트 PR.

### 스트레치
음성 실파일 데모 자산.

## UI 룰 (UI_UX_RUBRIC 프리즈 유지)

- 모바일(`/m`) = 고령 조사원: ≥18px 본문·≥48px 터치·한 화면 한 과업.
- 동 센터(`/center`) = 9급 공무원: 해석이 필요한 대시보드 금지, 모든 숫자에 다음 행동.
- 시·구(`/city`) = 정책 브리핑 톤.
- 안티-크린지(INV11) 유지. 섹션 제목은 할 일 지칭("오늘 배치 확인"·"보고 확인"·
  "통화 결과 입력").
- 시각 참조: 네이버지도(좌측 컨텐츠 패널 + 우측 지도, 칩형 필터, 접힘 패널).

## 루프 기계

워커 → 객관 게이트(불변식+done) → 저지(별도 컨텍스트, PASS/FAIL+사유) → Ralph 재확인
(스텁·무언 스코프컷·시맨틱 오류) → 커밋+PROGRESS+HANDOFF → 컨텍스트 리셋.
서킷브레이커: 페이즈당 재시도 5회 초과 시 멈추고 블로커 기록. 빌더는 자기완료 선언
금지, 저지가 프리즈 조건으로만 통과시킨다.

## 멈춤

페이즈 게이트+저지 통과 시에만 진행. P3까지 = 데모 성립(모바일→동 센터 스파인),
P4까지 = 3계층 완성. 막히면 멈추고 HANDOFF에 기록한다.
